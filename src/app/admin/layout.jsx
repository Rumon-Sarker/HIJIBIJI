import React from 'react';

const AdminLayout = ({children}) => {
    return (
        <div>
            <h1>admin layout</h1>
            {children}
        </div>
    );
};

export default AdminLayout;